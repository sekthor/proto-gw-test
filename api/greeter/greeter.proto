syntax = "proto3";

package sekthor.gateway.v1;

import "google/api/annotations.proto";

service Greeter {
    rpc Greet(GreetingRequest) returns (GreetingResponse);
}

message GreetingRequest {
    string name = 1;
}

message GreetingResponse {
    string name = 1;
}